apiVersion: v1
kind: Service
metadata:
  name: service-test
  labels:
    label-test: service-test
spec:
  selector:
    label-test: app-pod
  ports:
    - protocol: TCP
      name: metrics123
      port: 80
      targetPort: 8000
